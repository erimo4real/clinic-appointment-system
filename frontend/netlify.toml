[build]
  command = "CI=false npm run build"
  publish = "build"

[build.environment]
  DISABLE_ESLINT_PLUGIN = "true"

[[redirects]]
  from = "/api/*"
  to = "https://clinic-appointment-system-88np.onrender.com/api/:splat"
  status = 200
  force = true
